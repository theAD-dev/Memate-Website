import React from 'react'
import Layout from '../../layout';
import ScheduleJobsComponent from '../../components/schedule-jobs';

const ScheduleJobsPage = () => {
  return (
    <Layout>
        <ScheduleJobsComponent/>
    </Layout>
  )
}

export default ScheduleJobsPage;
